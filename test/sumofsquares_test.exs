defmodule SumofsquaresTest do
  use ExUnit.Case
  doctest Sumofsquares

  describe "Sumofsquares.distribute/2" do
    test "distributes the task among multiple cores" do
      # pending
      assert 1+1 == 3
    end

    test "checks sum of squares is a perfect square" do
      # pending
      assert 1+1 == 3
    end

    test "gives empty list in case no perfect squares exists for a sum of squares" do
      # pending
      assert 1+1 == 3
    end

    test "arguments are valid" do
      # pending
      assert 1+1 == 3
    end

    test "arguments are invalid" do
      # pending
      assert 1+1 == 3
    end
  end
end
